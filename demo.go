package main

//import "fmt"

func main() {
	// var x uint = 10
	// const y rune = -20 // Constants must be initialized at the time of declaration and cannot be changed later. They can be of any basic data type, including numeric types, strings, and booleans. Constants are often used to define values that should not change throughout the program, such as mathematical constants (e.g., Pi) or configuration values.
	// x = 30
	// var ch byte = 'A'
	// fmt.Println(x)
	// fmt.Println(y)
	// fmt.Println(ch)
	// fmt.Printf("%T", ch)
	// fmt.Println("\nThis is my first go program.")

	// var a bool = false
	// fmt.Println(a)

	// var str string = "Hello, World!"
	// fmt.Println(str)

	// var num float32 = 3.14
	// fmt.Println(num)

	// w := uint(324) //Implicit declaration with type inference
	// fmt.Println(w)
	// w = 0
	// fmt.Println(w)
	// fmt.Printf("%T\n", w)

	// formatFunc() //This comes from "formats_fmt.go" but since Go trearts all the files inside a folder like single big file , i don't have to explicitly tell the compiler to import it from that file . But when running , make sure to run both the files so , ' go run demo.go formats_fmt.go' or 'go run .'

	//operatorFunc()

	//tryString()

	//tryArrays()

	//trySlices()

	//tryMaps()

	tryFuncyions()
}
